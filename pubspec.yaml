name: persist_signals
description: A powerful persisted signals package for Flutter, built on top of signals_flutter
version: 0.0.1
homepage: https://github.com/davidtorosyan/persist_signals

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  signals: ^6.0.2
  path_provider: ^2.1.2
  path: ^1.8.3
  source_gen: ^2.0.0
  build: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4

flutter:
